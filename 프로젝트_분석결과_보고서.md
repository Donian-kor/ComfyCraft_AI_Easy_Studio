# 프로젝트 분석결과 보고서

> 작성일: 2026-09-09  
> 대상: ComfyUI + LM Studio 통합 이미지 생성 GUI v0.3  
> 분석 범위: 프로그램 코드, 화면 구성, 설정·워크플로우 파일, 문서, 의존성 목록, Git 추적 상태

## 1. 한눈에 보는 결론

이 프로젝트는 **LM Studio**와 **ComfyUI**를 한 화면에서 연결하여 이미지 생성 작업을 돕는 Windows 중심의 데스크톱 프로그램입니다.

- LM Studio는 사용자가 쓴 짧은 문장을 더 자세한 이미지 프롬프트로 바꾸는 역할을 합니다.
- ComfyUI는 프롬프트와 모델 설정을 받아 실제 이미지를 만드는 역할을 합니다.
- 프로그램 창은 **PySide6**로 만들었습니다. PySide6는 Python에서 데스크톱 화면을 만드는 도구입니다.
- 화면 디자인, 기능 코드, 설정 파일이 대체로 분리되어 있어 기능을 찾고 수정하기 쉬운 편입니다.
- 반면 현재 개발용 Python 실행 환경이 정상 동작하지 않았고, 자동 테스트가 없으며, 문서와 실제 설정 사이에 일부 차이가 확인되었습니다.

## 2. 프로젝트가 하는 일

사용자는 프로그램 화면에서 LM Studio와 ComfyUI의 주소를 확인하고, 모델과 이미지 크기·단계 수 등을 선택한 뒤 프롬프트를 입력합니다. 프로그램은 필요할 때 LM Studio에게 프롬프트 보완을 요청하고, 완성된 설정을 ComfyUI용 작업 지시서(JSON)로 만들어 전송합니다. 작업이 끝나면 결과 이미지를 `outputs/` 폴더에 저장하고 화면에 보여 줍니다.

### 사용 전 필요한 것

이 프로그램만 실행한다고 이미지가 생성되지는 않습니다. 다음 두 서비스가 별도로 준비되어 있어야 합니다.

| 필요한 프로그램 | 맡는 일 | 기본 연결 주소 |
| --- | --- | --- |
| LM Studio Local Server | 프롬프트 보완 | `http://127.0.0.1:1234` |
| ComfyUI 서버 | 이미지 생성 | `http://127.0.0.1:8188` |

기본 주소는 [`workflows/app_config.json`](workflows/app_config.json)에 들어 있습니다. 사용자의 설치 환경이 다르면 화면 또는 설정 파일에서 주소와 모델 경로를 맞춰야 합니다.

## 3. 전체 구조와 데이터 흐름

### 폴더 관계

```text
main.py                         프로그램 시작점과 화면 동작 연결
  │
  ├─ app/                       실제 기능 코드
  │   ├─ core/                  설정, 서버 통신, 모델·워크플로우 관리
  │   ├─ sections/              연결, 프롬프트, 생성, 결과, 진행 표시 기능
  │   └─ gui/                   화면 파일 읽기, 테마, 특수 버튼
  │
  ├─ assets/                    화면 디자인·아이콘·도움말
  │   └─ ui/main.ui             Qt Designer용 화면 설계 파일
  │
  ├─ workflows/                JSON 설정, 모델 기본값, ComfyUI 작업 양식
  │
  └─ outputs/                  생성된 이미지 저장 위치
```

### 이미지 생성 순서

```text
사용자 입력
  → 연결 확인 및 모델 목록 읽기
  → 모델별 기본값 적용
  → LM Studio로 프롬프트 보완(선택적)
  → JSON 워크플로우에 프롬프트·모델·크기 등의 값 채우기
  → ComfyUI에 작업 등록
  → WebSocket/HTTP로 진행 상태 확인
  → 결과 이미지 내려받기
  → outputs/ 저장 및 화면 미리보기
```

**WebSocket**은 서버가 작업 진행 상황을 프로그램에 바로 알려 주는 통신 방식입니다. 이 연결이 늦거나 실패해도, 프로그램은 HTTP 조회 방식으로 완료 여부를 확인하도록 구성되어 있습니다.

## 4. 주요 구성 요소

| 위치 | 역할 | 초보자용 주의점 |
| --- | --- | --- |
| [`main.py`](main.py) | 프로그램을 시작하고 버튼·입력창·각 기능을 연결합니다. | 가장 큰 제어 파일입니다. 화면 동작을 바꿀 때는 먼저 관련 기능이 `app/`에 이미 있는지 확인해야 합니다. |
| [`app/core/config_manager.py`](app/core/config_manager.py) | JSON 설정을 읽고 저장하며, 코드에서 사용하기 쉬운 형태로 바꿉니다. | 설정 항목을 새로 만들면 JSON 읽기와 저장 양쪽에 반영해야 합니다. |
| [`app/core/api_client.py`](app/core/api_client.py) | LM Studio·ComfyUI의 HTTP 요청과 ComfyUI 진행 상태 연결을 담당합니다. | 서버 주소나 통신 방식 변경은 이 파일과 연결 확인 기능을 함께 점검해야 합니다. |
| [`app/core/model_registry.py`](app/core/model_registry.py) | 모델 이름을 보고 적절한 모델 프로필을 고릅니다. | 새 모델은 이름 규칙과 워크플로우 종류를 함께 등록해야 합니다. |
| [`app/core/workflow_manager.py`](app/core/workflow_manager.py) | JSON 작업 양식을 읽고 입력값을 넣어 ComfyUI 요청 내용을 만듭니다. | JSON의 자리표시자 이름을 코드의 전달값과 맞춰야 합니다. |
| [`app/sections/prompt.py`](app/sections/prompt.py) | 프롬프트 정리·LM Studio 요청·응답 정리를 맡습니다. | 시스템 프롬프트는 코드가 아니라 `workflows/prompt.json`에 있습니다. |
| [`app/sections/generation.py`](app/sections/generation.py) | ComfyUI 작업 등록, 진행 추적, 취소, 결과 다운로드를 맡습니다. | 이미지 생성 중 화면이 멈추지 않도록 별도 작업 흐름을 사용합니다. |
| [`app/sections/connection.py`](app/sections/connection.py) | 서버 연결과 ComfyUI 모델 폴더를 확인합니다. | 자동 탐색보다 입력한 주소와 경로가 정확한지가 중요합니다. |
| [`assets/ui/main.ui`](assets/ui/main.ui) | Qt Designer로 만든 화면 배치와 위젯 이름을 담습니다. | 위젯의 `name`을 바꾸면 `main.py`에서 해당 위젯을 찾지 못할 수 있습니다. |
| [`workflows/`](workflows/) | 앱 설정, 프롬프트, 모델 기본값, ComfyUI 워크플로우 양식을 보관합니다. | JSON 문법 오류가 나면 프로그램 시작 또는 이미지 생성이 실패할 수 있습니다. |

## 5. 설정과 모델이 작동하는 방식

### 5.1 공통 설정: `app_config.json`

[`workflows/app_config.json`](workflows/app_config.json)은 다음과 같은 공통 값을 관리합니다.

- LM Studio와 ComfyUI의 주소, 시간 제한
- 이미지 가로·세로 크기, 단계 수, CFG 값의 기본값과 입력 범위
- 출력 폴더와 파일 이름 규칙
- 화면 크기, 글꼴, 해상도 빠른 선택 목록
- ComfyUI 모델 폴더 후보 경로
- 워크플로우 파일의 위치

프로그램은 이 파일을 읽어 화면 기본값에 적용하고, 설정 저장 기능을 통해 다시 기록할 수 있습니다.

### 5.2 모델 프로필: `default_profiles.json`

[`workflows/default_profiles.json`](workflows/default_profiles.json)은 Flux, ZImage, RealVisXL, JuggernautXL처럼 모델 종류별로 추천 단계 수, CFG, 샘플러, 스케줄러, 사용할 워크플로우를 지정합니다.

프로필은 모델 파일 이름에 포함된 단어를 이용해 선택합니다. 예를 들어 이름에 `flux`가 있으면 Flux용 워크플로우와 기본 설정을 우선 적용합니다. 이름 규칙에 맞지 않는 모델은 기본 checkpoint 방식으로 처리됩니다.

### 5.3 워크플로우 양식

`checkpoint.json`, `gguf_unet.json`, `flux_gguf.json`, `zimage.json` 등의 파일은 ComfyUI에 전달할 작업 순서를 JSON으로 정의합니다. `WorkflowManager`가 사용자 입력과 모델 프로필 값을 해당 양식에 넣은 뒤 ComfyUI로 보냅니다.

### 5.4 프롬프트 양식

[`workflows/prompt.json`](workflows/prompt.json)에는 Flux 계열과 SDXL 계열에 맞춘 시스템 프롬프트가 들어 있습니다. 화면의 짧은 문장을 LM Studio가 이미지 생성에 더 적합한 영어 문장 또는 태그 목록으로 보완할 때 사용합니다.

## 6. 기술 검토 결과

### 잘 구성된 점

| 항목 | 확인된 내용 | 평가 |
| --- | --- | --- |
| 기능 분리 | 연결·프롬프트·생성·결과·진행 표시가 `app/sections/`로 나뉘어 있습니다. | 기능별 위치가 분명해 유지보수에 유리합니다. |
| 화면과 디자인 분리 | 화면 배치는 `assets/ui/main.ui`, 테마는 QSS 파일, 동작 연결은 `main.py`에 있습니다. | 화면 모양 수정과 기능 수정의 충돌을 줄입니다. |
| 설정 분리 | 주소, 모델 기본값, 프롬프트, 워크플로우가 JSON 파일로 분리되어 있습니다. | 단순 설정 변경은 코드 수정 없이 할 수 있습니다. |
| 화면 멈춤 방지 | 모델 목록 조회와 생성 작업을 별도 스레드에서 처리하고, 신호로 화면에 결과를 전달합니다. | 오래 걸리는 이미지 생성 중에도 창이 멈출 가능성을 줄입니다. |
| 통신 대비책 | ComfyUI 작업 진행은 WebSocket을 우선 사용하고, 조회 방식도 함께 사용합니다. | 실시간 연결이 불안정해도 작업 완료 여부를 확인할 여지가 있습니다. |
| 오류 메시지 | 서버 연결·시간 초과·모델 미선택·ComfyUI 작업 오류를 사용자 메시지 또는 로그로 알립니다. | 문제 원인을 찾는 데 도움이 됩니다. |

### 구조상 주의할 점

- `main.py`의 `MainController`는 화면 연결, 설정 저장, 연결 확인, 모델 목록, 프롬프트 보완, 생성 시작·종료 등을 모두 담당합니다. 약 1,400줄로 커졌기 때문에 앞으로 기능이 늘면 수정 위치를 찾기 어려워질 수 있습니다.
- `GenerationWorker`는 별도 작업 흐름을 사용하지만, 일부 구간에서 `QApplication.processEvents()`로 화면 이벤트를 직접 처리합니다. 현재 동작을 유지하되, 추후 화면 객체에 직접 접근하는 부분은 신중히 점검하는 것이 좋습니다.
- 서버 통신 코드가 `api_client.py`, `model_fetcher.py`, `connection.py`, `prompt.py`에 나뉘어 있습니다. 역할은 다르지만 요청 재시도·시간 제한·오류 기록 방식이 조금씩 달라질 수 있습니다.

## 7. 확인된 주의점과 개선 제안

아래 항목은 초기 분석에서 실제 파일과 실행 확인을 통해 찾은 내용입니다. 바로 다음 표에 각 항목의 처리 상태를 기록했습니다.

| 우선순위 | 무엇이 문제인가 | 왜 영향이 있는가 | 권장 조치 |
| --- | --- | --- | --- |
| 높음 | `.venv\\Scripts\\python.exe`는 존재하지만 실행 시 원본 Python 3.14 위치를 찾지 못해 문법 검사 실행에 실패했습니다. | 개발·검증 명령을 실행할 수 없어, 변경 후 오류를 미리 확인하기 어렵습니다. | 가상환경을 다시 만들고 최소 실행 의존성을 설치한 뒤 `python -m compileall main.py app` 및 프로그램 실행을 확인합니다. |
| 높음 | 자동 테스트 파일과 테스트 설정이 확인되지 않았습니다. | 모델·워크플로우·설정 변경 뒤 기존 기능이 깨져도 자동으로 알아내기 어렵습니다. | 설정 읽기, 모델 프로필 선택, 워크플로우 생성, 응답 해석부터 작은 자동 테스트를 추가합니다. |
| 중간 | 도움말 문서에는 LM Studio 기본 포트가 `1729`라고 적힌 부분이 있으나, 실제 기본 설정과 코드 기본값은 `1234`입니다. | 초보자가 잘못된 주소로 서버를 실행하거나 연결 실패 원인을 찾기 어려울 수 있습니다. | `assets/help/INSTALLATION.md`와 실제 `app_config.json`·코드 기본값을 같은 기준으로 맞춥니다. |
| 중간 | 도움말 문서에는 모델 프로필 위치가 `src/model_profiles/`로 표기되어 있지만 실제 위치는 `app/core/model_profiles/`입니다. | 안내를 따라 파일을 찾거나 수정하려는 사용자가 혼란을 겪을 수 있습니다. | `assets/help/README.md`의 경로를 실제 프로젝트 구조로 수정합니다. |
| 중간 | `requirements.txt`에는 105개 패키지가 적혀 있지만, 프로젝트 안내의 최소 실행 목록은 5개입니다. PyQt5·PyQt6·PySide6처럼 같은 목적의 화면 도구도 함께 들어 있습니다. | 설치 시간이 길어지고, 서로 다른 화면 도구가 섞여 환경 충돌이나 관리 혼란이 생길 수 있습니다. | 실제 사용 모듈을 기준으로 실행용 의존성 목록을 정리하고, 개발용 목록은 별도 파일로 분리합니다. |
| 낮음 | Python 캐시 파일인 `__pycache__/*.pyc`가 Git에서 추적되고 있습니다. | 소스 코드가 아닌 자동 생성 파일이 변경 기록에 섞여 검토가 불편해질 수 있습니다. | `.gitignore`에 `__pycache__/`, `*.pyc`를 추가하고, 별도 작업에서 이미 추적 중인 캐시 파일을 정리합니다. |
| 낮음 | 프로젝트 안내 문서에는 v0.3으로 표시되지만 `app/__init__.py`의 `__version__`은 `0.2.0`입니다. | 화면·문서·문제 보고에서 버전 기준이 엇갈릴 수 있습니다. | 다음 버전 정리 때 문서와 코드의 버전 값을 한 기준으로 통일합니다. |

### 개선 조치 진행 상태 (2026-09-09)

| 항목 | 상태 | 처리 내용 |
| --- | --- | --- |
| 문서의 포트·경로 | 완료 | LM Studio 기본 주소를 `1234`로 통일하고, 모델 프로필과 설정 파일의 실제 경로를 반영했습니다. |
| 코드 버전 | 완료 | `app/__init__.py`의 버전을 `0.3.0`으로 맞췄습니다. |
| 실행 의존성 | 완료 | `requirements.txt`를 실제 직접 사용 패키지 4개로 정리했습니다. |
| 기본 자동 테스트 | 완료 | 외부 서버 없이 JSON, 설정 경로, 모델 프로필, 버전을 확인하는 테스트를 추가했습니다. |
| Python 캐시 | 완료 | 새 `.gitignore` 규칙을 추가하고, 이미 Git에서 추적하던 캐시 파일을 추적 대상에서 제거했습니다. |
| Python 가상환경 | 보류 | 사용 가능한 일반 Python 설치본이 없어 `.venv` 재생성은 진행할 수 없습니다. Python 3.10 이상을 설치한 뒤 가상환경을 다시 만들면 됩니다. |

## 8. 검증 결과와 한계

### 확인한 항목

| 확인 항목 | 결과 | 방법 |
| --- | --- | --- |
| JSON 파일 형식 | 성공 | `workflows/`의 JSON 파일 10개를 읽어 JSON 형식으로 해석 가능한지 확인했습니다. |
| 프로젝트 구조 | 성공 | `main.py`, `app/`, `assets/`, `workflows/` 및 관련 호출 관계를 읽어 확인했습니다. |
| 화면 구성 | 확인 | `assets/ui/main.ui`에서 사이드바, 연결·생성·프롬프트·결과 관련 화면 요소를 확인했습니다. |
| Git 캐시 추적 | 완료 | `.gitignore`를 추가하고, Git에서 추적하던 Python 캐시 파일 26개를 추적 대상에서 해제했습니다. 파일 자체는 디스크에 남아 있습니다. |
| Python 문법 검사 | 성공 | Comfy Desktop에 포함된 Python 3.13으로 `main.py`, `app/`, `tests/`의 문법을 검사했습니다. |
| 기본 자동 테스트 실행 | 미완료 | 새 테스트 파일은 문법 검사에 통과했지만, 사용할 수 있는 Python 실행본에 `unittest` 모듈이 없어 실행하지 못했습니다. JSON·설정·모델 프로필의 같은 핵심 조건은 수동 검사로 통과했습니다. |
| 실제 이미지 생성 | 미실행 | LM Studio·ComfyUI 서버 및 모델이 준비된 상태가 아니므로 실제 서버 통신과 이미지 생성은 검증하지 않았습니다. |

## 9. 초보자를 위한 다음 작업 순서

1. 먼저 Python 가상환경을 정상화해 프로그램을 실행할 수 있는 상태를 만듭니다.
2. 문서의 포트·폴더 경로·버전 표기를 실제 코드와 맞춥니다.
3. 최소 의존성 목록을 기준으로 깨끗한 환경에서 설치와 실행을 확인합니다.
4. 설정 읽기와 워크플로우 생성부터 자동 테스트를 추가합니다.
5. 이후에 `main.py`의 화면 제어 기능을 연결·설정·생성처럼 더 작은 단위로 나누는 개선을 검토합니다.

## 10. 참고 파일

- 프로그램 시작과 화면 제어: [`main.py`](main.py)
- 기능 모듈 내보내기와 코드 버전: [`app/__init__.py`](app/__init__.py)
- 설정 읽기·저장: [`app/core/config_manager.py`](app/core/config_manager.py)
- 서버 통신: [`app/core/api_client.py`](app/core/api_client.py)
- 모델 판단과 워크플로우 생성: [`app/core/model_registry.py`](app/core/model_registry.py), [`app/core/workflow_manager.py`](app/core/workflow_manager.py)
- 이미지 생성: [`app/sections/generation.py`](app/sections/generation.py)
- 화면 설계: [`assets/ui/main.ui`](assets/ui/main.ui)
- 사용자 설정과 모델 기본값: [`workflows/app_config.json`](workflows/app_config.json), [`workflows/default_profiles.json`](workflows/default_profiles.json)
- 기존 도움말: [`assets/help/README.md`](assets/help/README.md), [`assets/help/INSTALLATION.md`](assets/help/INSTALLATION.md)
