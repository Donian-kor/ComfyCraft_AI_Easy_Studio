# 🔍 ComfyCraft AI Easy Studio v0.3 — 코드 리뷰 보고서

> **분석 대상**: 프로젝트 전체 (약 30개 소스 파일, 10개 JSON 설정, 113KB UI 파일)
> **분석 일시**: 2026-09-10

---

## 📁 프로젝트 구조 개요

```mermaid
flowchart TD
    main["main.py (2,139줄)\n메인 컨트롤러"]
    
    subgraph app["app 패키지"]
        init["__init__.py\n(전체 API 중앙 export)"]
        
        subgraph core["core 모듈"]
            api["api_client.py\nHTTP/WS 클라이언트"]
            cfg["config_manager.py\n설정 로드/저장"]
            pool["connection_pool.py\n연결 풀"]
            fetch["model_fetcher.py\n모델 목록 조회"]
            reg["model_registry.py\n모델 프로파일 매칭"]
            mss["model_status_service.py\n연결 상태 서비스"]
            wm["workflow_manager.py\n워크플로우 렌더링"]
        end

        subgraph gui["gui 모듈"]
            psb["play_stop_button.py\n커스텀 생성/정지 버튼"]
            stb["split_text_button.py\n분할 텍스트 버튼"]
            tm["theme_manager.py\n테마 관리"]
            ul["ui_loader.py\nUI 파일 로더"]
        end

        subgraph sections["sections 모듈"]
            conn["connection.py\n연결 확인"]
            exec["execution.py\n진행 상태/타이머"]
            gen["generation.py\n이미지 생성 워커"]
            prom["prompt.py\n프롬프트 처리"]
            res["result.py\n결과 이미지 관리"]
        end
    end

    main --> init
    init --> core
    init --> sections
    main --> gui
    gen --> api
    gen --> wm
    gen --> reg
```

전체적으로 **모듈 분리가 잘 되어 있고**, 기능별로 역할이 명확하게 나뉘어 있습니다.

---

## 🚨 심각도 높음 — 실제 버그 또는 런타임 오류 가능성

### 1. `check_connection_silent` — 존재하지 않는 모듈 import ‼️

[connection.py](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/app/sections/connection.py#L190-L198)

```python
def check_connection_silent(service: str, url: str, timeout: float = 0.3) -> bool:
    try:
        from src import LMStudioApiClient, ComfyUIApiClient  # ← ❌ 존재하지 않는 모듈!
```

**문제**: `from src import ...`는 이 프로젝트에 `src` 패키지가 없기 때문에 **항상 `ImportError`가 발생**하고, `except Exception`에 의해 **항상 `False`를 반환**합니다. 즉 이 함수는 어떤 서버에 대해서도 **항상 "연결 실패"**를 반환합니다.

**영향 범위**: [main.py:626](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L626)의 `refresh_models()` 내 `fetch()` 스레드에서 호출되어, 초기 로딩 시 연결 상태가 항상 실패로 표시될 수 있습니다. 하지만 `check_connection()` 메서드(L690-726)가 별도로 직접 API를 호출하므로, 사용자가 "연결 확인" 버튼을 누르면 정상 동작합니다.

**수정 방향**: `from src import` → `from app.core.api_client import`

---

### 2. 백그라운드 스레드에서 `QApplication.processEvents()` 호출 ⚠️

[generation.py:159](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/app/sections/generation.py#L157-L159), [194](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/app/sections/generation.py#L194), [244](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/app/sections/generation.py#L244)

```python
# generation.py 내 GenerationWorker.generate() (백그라운드 threading.Thread에서 실행됨)
for _ in range(5):
    time.sleep(0.05)
    QApplication.processEvents()  # ← ⚠️ 메인 스레드가 아닌 곳에서 호출
```

**문제**: `GenerationWorker.run()`은 [main.py:1534](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1534)에서 `threading.Thread(target=self.worker.run, daemon=True)`로 실행됩니다. Qt의 `processEvents()`는 **메인(GUI) 스레드에서만 안전**하게 호출할 수 있습니다. 백그라운드 스레드에서 호출하면 **간헐적인 크래시나 데이터 손상**이 발생할 수 있습니다.

**현재 상태**: 운 좋게 크래시가 자주 발생하지 않을 수 있지만, Qt 공식 문서에 따르면 이것은 **정의되지 않은 동작(Undefined Behavior)**입니다.

**수정 방향**: `QApplication.processEvents()` 호출을 모두 제거하거나, 필요한 경우 `QMetaObject.invokeMethod()`를 사용하여 메인 스레드에 위임해야 합니다.

---

### 3. ConfigManager 내부 속성 직접 접근 (`_config`)

[main.py:908](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L908), [1097](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1097)

```python
self.config_manager._config = self.config  # ← private 속성 직접 수정
```

**문제**: `_config`는 `ConfigManager`의 내부(private) 속성입니다. 외부에서 직접 수정하면 `ConfigManager`의 내부 상태가 일관성을 잃을 수 있습니다. `ConfigManager`에 `set_config()` 같은 공개 메서드를 추가하는 것이 안전합니다.

---

## ⚠️ 심각도 중간 — 코드 품질 및 유지보수 문제

### 4. `main.py`가 너무 큼 (2,139줄, 99KB)

[main.py](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py)의 `MainController` 클래스는 **모든 UI 로직을 하나의 클래스에** 담고 있습니다.

| 메서드/영역 | 줄 수 | 비고 |
|---|---|---|
| `setup()` | ~250줄 | 위젯 초기화 + 시그널 연결 전부 |
| `capture_snapshot()` + FaceDetailer | ~110줄 | 스냅샷 생성 |
| `_restore_facedetailer_from_snapshot()` | ~100줄 | 스냅샷 복원 |
| `_setup_new_studio_features()` | ~130줄 | 추가 UI 기능 |
| `_show_facedetailer_guide()` | ~80줄 | HTML 하드코딩 |
| `_show_settings_dialog()` | ~75줄 | 다이얼로그 생성 |

**개선 방향**: 
- FaceDetailer 관련 로직을 별도 클래스/모듈로 분리
- 다이얼로그 코드를 별도 파일로 분리
- `setup()`을 기능별 소규모 메서드로 분해

---

### 5. FaceDetailer 코드 반복 패턴

[capture_snapshot()](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1167-L1272)와 [_restore_facedetailer_from_snapshot()](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1274-L1369)에서 17개 FaceDetailer 옵션에 대해 **거의 동일한 코드가 반복**됩니다.

```python
# capture_snapshot 내부 — 17개 각각에 대해 유사한 코드
facedetailer_denoise = _fd_val("facedetailerDenoiseSlider", 0.40, factor=100.0)
facedetailer_steps = _fd_val("facedetailerStepsSlider", 20)
# ... 15개 더

# _restore_facedetailer_from_snapshot 내부 — 17개 각각에 대해 if + try/except
if "facedetailer_denoise" in snap:
    try:
        self._set_fd_slider("facedetailerDenoiseSlider", round(float(snap["facedetailer_denoise"]) * 100))
    except Exception:
        pass
# ... 15개 더
```

**개선 방향**: 옵션 목록을 데이터 구조(리스트/딕셔너리)로 정의하고 루프로 처리하면 코드량을 약 80% 줄일 수 있습니다.

---

### 6. 하드코딩된 HTML 문자열 (도움말/가이드)

[main.py:1987-2045](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1987-L2045)에 FaceDetailer 가이드 HTML이 **파이썬 코드 안에 직접 포함**되어 있습니다. 약 60줄의 HTML 테이블이 파이썬 문자열로 들어있어 수정하기 어렵습니다.

**개선 방향**: `assets/help/facedetailer_guide.html` 같은 별도 파일로 분리

---

### 7. 도움말 로딩 코드 중복

[_setup_help_tab()](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L985-L1048)과 [_show_help_dialog()](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1927-L1974)에서 **같은 README.md 파일을 거의 같은 방식으로 읽고 HTML로 변환**하는 코드가 반복됩니다. markdown 패키지 import도 두 곳에서 각각 합니다.

---

### 8. `show_message_box`에 하드코딩된 다크테마 스타일

[result.py:103-127](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/app/sections/result.py#L103-L127)

```python
msg_box.setStyleSheet("""
    QMessageBox { background-color: #1f2937; color: #e2e8f0; }
    ...
""")
```

**문제**: 프로젝트에는 9개의 테마가 있지만, 메시지 박스는 **항상 다크테마 색상을 사용**합니다. 라이트테마를 사용할 때 어색하게 보일 수 있습니다.

---

## 💡 심각도 낮음 — 개선 제안

### 9. 중복된 requirements.txt

[requirements.txt](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/requirements.txt)와 [comfyui_lmstudio_requirements.txt](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/comfyui_lmstudio_requirements.txt)의 내용이 **완전히 동일**합니다. 하나로 통합하거나, 둘의 목적을 구분하는 것이 좋습니다.

---

### 10. `ConnectionStatus`의 일본어 기본값

[connection.py:23](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/app/sections/connection.py#L23)

```python
@dataclass
class ConnectionStatus:
    message: str = "未確認"  # ← 일본어 "미확인"
```

프로젝트 전체가 한국어인데, 이 기본값만 일본어입니다. `"미확인"`으로 바꾸는 것이 일관성에 맞습니다. (실제로 이 기본값은 사용 전 항상 덮어씌워지므로 기능에 영향은 없습니다.)

---

### 11. 스레드 안전성 — `self.worker` 접근

[main.py:1489-1534](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1489-L1534)

```python
def start_generation(self):
    if self.worker:  # 메인 스레드에서 확인
        return
    ...
    self.worker = GenerationWorker(...)
    threading.Thread(target=self.worker.run, daemon=True).start()

def generation_finished(self, success):
    self.worker = None  # 시그널 콜백에서 설정
```

`self.worker`에 대한 접근이 잠금(lock) 없이 여러 스레드에서 이루어집니다. 현재는 Qt Signal 체계 덕분에 `generation_finished`가 메인 스레드에서 호출되므로 대부분 안전하지만, `start_generation`이 매우 빠르게 연속 호출되면 경쟁 상태(race condition)가 발생할 수 있습니다.

---

### 12. 디버그 로그가 프로덕션에 남아 있음

[main.py](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py) 곳곳에 `[DEBUG]` 로그가 남아 있습니다:

- L1372: `"[DEBUG] enhance_prompt_only 호출됨"`
- L1386: `"[DEBUG] lm_url=..."`
- L1398: `"[DEBUG] is_lm_connected=..."`
- L1458: `"[DEBUG] 워커 시작..."`
- L1463: `"[DEBUG] 프롬프트 향상 성공:..."`

사용자가 로그창에서 이 디버그 메시지를 볼 수 있습니다. 로그 레벨 시스템(예: Python logging 모듈)을 도입하면 개발/프로덕션을 구분할 수 있습니다.

---

### 13. 일부 `except Exception: pass` 패턴

[main.py:1137-1138](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1137-L1138), [1283-1284](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1283-L1284), [1611-1612](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/main.py#L1611-L1612)

```python
except Exception:
    pass
```

오류를 완전히 삼키면 디버깅이 어렵습니다. 최소한 `print()` 또는 `logging.debug()`로 기록하는 것이 좋습니다.

---

### 14. 테스트 커버리지 부족

현재 테스트는 [test_project_basics.py](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/tests/test_project_basics.py) 1개 파일뿐이며, JSON 파일 유효성과 버전 문자열만 검사합니다. 핵심 비즈니스 로직(프롬프트 처리, 워크플로우 렌더링, 모델 프로파일 매칭 등)에 대한 유닛 테스트가 없습니다.

---

### 15. `PlayStopButton` 텍스트 불일치

[play_stop_button.py:58](file:///c:/Users/donian/Desktop/python/comfyui_Lmstudio_gui%20v0.3/app/gui/play_stop_button.py#L58)

```python
def _on_toggled(self, checked):
    self.setText("이미지생성 시작" if checked else "정 지")
    #                                              ↑ 공백 있음 "정 지"
```

반면 L50에서는: `self.setText("이미지생성 시작")` — 체크됐을 때와 안 됐을 때의 텍스트 로직이 **반전**되어 있고, "정 지"에 불필요한 공백이 있습니다.

---

## ✅ 잘 된 점

| 영역 | 평가 |
|---|---|
| **모듈 분리** | core/gui/sections 3계층으로 잘 나뉨 |
| **설정 관리** | dataclass 기반 AppConfig로 타입 안전한 설정 |
| **모델 프로파일** | 자동 감지 + JSON 기반 확장 가능한 구조 |
| **워크플로우 템플릿** | 플레이스홀더 기반 렌더링으로 유연한 워크플로우 |
| **테마 시스템** | 9개 테마, QSS 기반, 설정 저장 |
| **API 클라이언트** | 자동 재시도, 세션 관리 포함 |
| **캐싱** | TTL 기반 모델 목록 캐시 (model_fetcher) |
| **UI 로더** | 커스텀 위젯 자동 교체 (PlayStopButton, SplitTextButton) |
| **한국어 UX** | 모든 UI 텍스트와 오류 메시지가 한국어 |

---

## 📊 요약 — 우선순위별 분류

| 우선순위 | 항목 | 설명 |
|---|---|---|
| 🔴 높음 | #1 `check_connection_silent` | 잘못된 import로 항상 실패 반환 |
| 🔴 높음 | #2 `processEvents()` | 백그라운드 스레드에서 Qt 이벤트 처리 |
| 🟡 중간 | #3 `_config` 직접 접근 | private 속성 외부 수정 |
| 🟡 중간 | #4 main.py 크기 | 2,139줄 단일 파일 |
| 🟡 중간 | #5 FaceDetailer 반복 | 동일 패턴 17회 반복 |
| 🟡 중간 | #8 메시지박스 테마 | 다크테마 하드코딩 |
| 🟢 낮음 | #6 HTML 하드코딩 | 도움말을 별도 파일로 |
| 🟢 낮음 | #7 도움말 코드 중복 | 2곳에서 같은 로직 |
| 🟢 낮음 | #9 중복 requirements | 파일 2개가 동일 |
| 🟢 낮음 | #10 일본어 기본값 | "未確認" → "미확인" |
| 🟢 낮음 | #11 스레드 안전성 | worker 접근 보호 |
| 🟢 낮음 | #12 디버그 로그 | 프로덕션에 DEBUG 메시지 |
| 🟢 낮음 | #13 예외 삼킴 | `except: pass` 패턴 |
| 🟢 낮음 | #14 테스트 부족 | 핵심 로직 테스트 없음 |
| 🟢 낮음 | #15 버튼 텍스트 | 텍스트 반전 + 공백 |
