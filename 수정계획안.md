# 죽은 UI 정리 — `hiddenSettingsContainer` 제거 계획서

- 대상 프로젝트: `ComfyCraft_AI_Easy_Studio`
- 작성 기준: `main` 브랜치 / `fcd4968` (2026-09-26)
- 목적: `assets/ui/main.ui`의 `hiddenSettingsContainer` 및 그 안에 남은
  구버전 UI 잔재를 정리하고, 보이지 않는 임시 위젯 의존을 제거한다.
- 범위 외: `IMG2IMG_PLAN.md` (보류), 위젯 배치·레이아웃 redesign

---

## 0. 요약 (Executive Summary)

`hiddenSettingsContainer`는 Qt Designer로 UI를 만들던 시절의 **설정 패널**이다.
이후 `settings_dialog.ui` (별도 QDialog)가 등장하면서 완전히 대체되었고,
컨테이너는 `visible=false`로 방치된 채 **"보이지 않지만 데이터는 오가는"** 상태로 남았다.

| 항목 | 값 |
|---|---|
| 컨테이너 존재 | `main.ui:2567` |
| 가시성 | `visible=false` (실측 확인) |
| 크기 / 부모 | 100×30 / `centralWidget` |
| 레이아웃 부착 | **어떤 레이아웃에도 추가되지 않음** (유령 컨테이너) |
| 내부 위젯 | 14개 (`settingsLayout` 자식, 실측 확인) |
| 대체 UI | `assets/ui/settings_dialog.ui` (별도 QDialog, `dlg*` 접두사) |
| 현재 테스트 | `unittest` 20개 전부 통과 (6.8s) |

**교차검증 결과, 14개 중 3개만 실제로 살아있고 11개는 죽었다.**

---

## 1. 교차검증 (2중 확인)

### 1.1 정적 분석 — 코드 참조 위치 전수 조사

| 위젯 | 참조 수 | 실제 용도 | 판정 |
|---|---|---|---|
| `lmUrlEdit` | 15 | `save_config`(1540), `capture_snapshot`(1723), `refresh_models`(1023), `check_connection`(1160/1167), `load_config`(1418), 설정창 복사(2470/2488/2522/2556), `_set_ui_enabled`(2034) | 🟢 그룹A |
| `comfyUrlEdit` | 14 | 동일 패턴 (1544, 1725, 1024, 1419, 2476, 2545 …) | 🟢 그룹A |
| `comfyModelPathEdit` | 9 | `save_config`(1550), `browse_model_folder`(1256), `load_config`(1432), 설정창(2466/2502/2516/2549) | 🟢 그룹A |
| `lmStatusLabel` | 4 | 1041(갱신), 1210(읽기), 1218(갱신) | 🔴 그룹C |
| `comfyStatusLabel` | 3 | 1055, 1218 | 🔴 그룹C |
| `modelPathStatusLabel` | 2 | 1286(기본값 인자) | 🔴 그룹C |
| `lmCheckButton` | 2 | 351(`clicked.connect`만), 2026(targets) | 🔴 그룹B |
| `comfyCheckButton` | 2 | 354, 2027 | 🔴 그룹B |
| `browseModelFolderButton` | 2 | 357, 2028 | 🔴 그룹B |
| `loadConfigButton` | 2 | 340, 2031 | 🔴 그룹B |
| `saveConfigButton` | 2 | 341, 2032 | 🔴 그룹B |
| `restoreDefaultsButton` | 2 | 342, 2033 | 🔴 그룹B |
| `exitButton` | 3 | 345, 2056, (split_text_button.py:68 주석) | 🔴 그룹B |
| `helpBrowser` | 2 | 1500(`setHtml`로 내용 채움) | 🔴 그룹C |
| `settingsLayout` | 1 | 508(themeComboBox 부재 시 폴백) | 🔴 그룹C |

### 1.2 실행 프로브 — 실제 로드 후 위젯 트리 검사

`QT_QPA_PLATFORM=offscreen` + `load_ui("assets/ui/main.ui")`로 실측:

```
컨테이너 존재: True
  visible=False  size=100x30  부모=centralWidget
settingsLayout 존재: True, 자식수=14
   [0] comfyUrlEdit           [7] lmCheckButton
   [1] comfyCheckButton       [8] lmStatusLabel
   [2] comfyStatusLabel       [9] loadConfigButton
   [3] comfyModelPathEdit     [10] saveConfigButton
   [4] browseModelFolderButton [11] restoreDefaultsButton
   [5] modelPathStatusLabel   [12] exitButton
   [6] lmUrlEdit              [13] helpBrowser

모든 14개 위젯의 상위 체인:  <이름>[H] <- hiddenSettingsContainer[H] <- centralWidget[H]
themeComboBox 존재=True   (main.ui:115 — 컨테이너 외부, 정상 노출)
```

**해석**: `H` = Hidden. 컨테이너가 죽어 있으므로 **자식 14개 전부 사용 불가 상태**다.
설정 상태를 갱신하는 1041·1055·1218·1286 라벨 코드는 **아무 화면에도 반영되지 않는다.**

### 1.3 대체 UI 확인 — `settings_dialog.ui` 1:1 대응

| 구버전 (숨김) | 현행 설정창 | 상태 |
|---|---|---|
| `lmUrlEdit` | `dlgLmUrlEdit` | 완전 대체 |
| `comfyUrlEdit` | `dlgComfyUrlEdit` | 완전 대체 |
| `comfyModelPathEdit` | `dlgModelPathEdit` | 완전 대체 |
| `browseModelFolderButton` | `dlgBrowseBtn` | 완전 대체 |
| `modelPathStatusLabel` | `dlgModelPathStatusLabel` | 완전 대체 |
| `lmCheckButton` / `comfyCheckButton` | `dlgLmCheckBtn` / `dlgComfyCheckBtn` | 완전 대체 |
| `lmStatusLabel` / `comfyStatusLabel` | `dlgLmStatusLabel` / `dlgComfyStatusLabel` | 완전 대체 |
| `loadConfigButton` | `dlgLoadConfigBtn` (2508) | 완전 대체 |
| `saveConfigButton` | `dlgSaveCloseBtn` (2541) | 완전 대체 |
| `restoreDefaultsButton` | `dlgResetDefaultsBtn` (2526) | 완전 대체 |
| `exitButton` | — (창 X 버튼 `eventFilter`로 대체) | 대체됨 |
| `helpBrowser` | `help_dialog_v2.ui` (좌측 도움말 버튼) | 대체됨 |

**결론: 14개 중 살아있는 것은 그룹A 3개뿐이다.**

---

## 2. 판정 그룹

### 그룹A — 데이터 보관용 (지우면 안 됨) · 3개

`lmUrlEdit`, `comfyUrlEdit`, `comfyModelPathEdit`

세션 중 "아직 저장 전" 설정값을 보관하는 **보이지 않는 임시 저장소**다.
값의 흐름이 3-way로 꼬여 있다:

```
app_config.json ──load──▶ self.config ──복사──▶ [그룹A 위젯] ──복사──▶ 설정창
       ▲                     │                      │                      │
       └──────save───────────┴──────save_config────┴──────on_save_close──┘
```

`self.config`는 `save_config()` 호출 시점에만 파일로 기록되고,
그 전까지는 그룹A 위젯이 "수정 대기 중인 값"을 들고 있는 역할이다.

### 그룹B — 죽음 (시그널 연결만, 표시 불가) · 7개

`lmCheckButton`, `comfyCheckButton`, `browseModelFolderButton`,
`loadConfigButton`, `saveConfigButton`, `restoreDefaultsButton`, `exitButton`

버튼이 존재하나 부모가 숨겨져 있어 **클릭 자체가 불가능**하다.
`clicked.connect`만 걸어둔 상태이며, 설정창 버튼이 그 기능을 전부 수행한다.

### 그룹C — 죽음 (표시·기능 모두 무효) · 4개

| 위젯 | 죽은 이유 |
|---|---|
| `lmStatusLabel`, `comfyStatusLabel` | 갱신만 하고 표시 불가. 배지 버튼(`lmStatusBtn`/`comfyStatusBtn`)이 실제 표시 담당 |
| `modelPathStatusLabel` | 기본 인자로만 사용(1286). 설정창은 `dlgModelPathStatusLabel`을 명시적으로 전달 |
| `helpBrowser` | `_setup_help_tab()`(1498)이 HTML을 채우지만 부모가 숨김 → 표시 불가. `help_dialog_v2.ui`가 담당 |
| `settingsLayout` | `_setup_theme_selector()`(492~506)가 `themeComboBox` 존재 시 `return` → 508 이후 **미실행 코드** |

---

## 3. 수정 계획 (3단계)

### 0단계 — 안전 장치 (필수, 코드 변경 없음)

| 항목 | 명령 |
|---|---|
| 현재 작업 트리 커밋 | `git add -A && git commit -m "chore: 죽은 UI 정리 착수 전 백업"` |
| 사용자 설정 백업 | `Copy-Item workflows\app_config.json workflows\app_config.json.bak` |
| 무결성 확인 기준 | 커밋 후 `git diff --stat` 비어있을 것 |

> `app_config.json`은 **사용자 실설정**이므로 3단계 전까지 절대 삭제/재생성 금지.

### 1단계 — 그룹B · 그룹C 제거 (위험도 🟢 낮음)

**대상 파일 3개**

| 파일 | 작업 |
|---|---|
| `assets/ui/main.ui` | 2567~2639 컨테이너 블록에서 14개 중 11개 위젯 제거 → 그룹A 3개만 유지 |
| `assets/ui/main_ui.py` | 대응 11개 생성 블록 제거 |
| `main.py` | 아래 표의 시그널·갱신 코드 제거 |

**`main.py` 제거 대상 라인 (1단계)**

| 라인 | 내용 | 처리 |
|---|---|---|
| 340~345 | `loadConfigButton` / `saveConfigButton` / `restoreDefaultsButton` / `exitButton` 시그널 연결 | **삭제** |
| 351~359 | `lmCheckButton` / `comfyCheckButton` / `browseModelFolderButton` 시그널 연결 | **삭제** |
| 1041~1046 | `lmStatusLabel` 갱신 블록 (`if lm_label:` 가드 포함) | **삭제** |
| 1055~1060 | `comfyStatusLabel` 갱신 블록 | **삭제** |
| 2026~2028 | targets 내 3개 버튼명 | **삭제** |
| 2031~2033 | targets 내 3개 설정 버튼명 | **삭제** |
| 2056 | targets 내 `exitButton` | **삭제** |
| 1210~1211 | `is_lm_connected()`의 라벨 텍스트 판정 | ⚠️ **유지** (2단계에서 처리) |
| 1217~1219 | `update_connection_label()`의 `label` 조회 | ⚠️ **유지** (`_apply_status_label`이 `None` 가드 보유) |
| 1286 | `update_model_path_status()`의 기본 label | ⚠️ **유지** (`_apply_status_label`이 `None` 가드 보유) |

**`_set_ui_enabled()` targets 삭제 근거 (2026~2056)**

```python
for name in targets:
    for widget in self.window.findChildren(QWidget, name):   # 2066
        self._ui_enabled_widgets.append(widget)
```

`findChildren`는 **없으면 빈 리스트**를 반환하므로 `None` 크래시는 없다.
다만 죽은 이름을 남겨두면 설정 진입점의 의도가 코드에서 사라지지 않으므로 함께 정리한다.

**예상 작업량**: `main.ui` 약 50줄 / `main_ui.py` 약 55줄 / `main.py` 약 50줄 삭제

### 2단계 — 그룹A 정리: 위젯 → `self.config` 직접 참조 (위험도 🟡 중간)

**목적**: 컨테이너를 완전히 없애고, 임시 저장소 의존을 제거한다.

**변환 규칙**

| 기존 | 대체 |
|---|---|
| `self.find(QLineEdit, "lmUrlEdit").text().strip()` | `self.config.lmstudio.url` |
| `self.find(QLineEdit, "comfyUrlEdit").text().strip()` | `self.config.comfyui.url` |
| `self.find(QLineEdit, "comfyModelPathEdit").text().strip()` | `self.config.comfyui_model_paths[0]` (빈 목록 시 `""`) |
| `self.find(QLineEdit, "lmUrlEdit").setText(v)` | `self.config.lmstudio.url = v` |

**`main.py` 수정 대상 라인 (2단계)**

| 라인 | 메서드 | 용도 |
|---|---|---|
| 262~266 | `setup()` | URL 초기 표시 + validator → 위젯 제거 대상이므로 정리 |
| 267 | `setup()` | `comfyUrlEdit` 초기 표시 → **삭제** |
| 270 | `setup()` | 모델 경로 초기 표시 → **삭제** |
| 1023~1024 | `refresh_models()` | URL **읽기** → `self.config` 참조 |
| 1035, 1049 | `refresh_models()` | URL **쓰기** → `self.config` 대입 |
| 1160, 1167~1168 | `check_connection()` | URL 읽기/쓰기 → `self.config` |
| 1418~1419 | `load_config()` | `self.config`→위젯 복사 → **삭제** (이미 `self.config`에 있음) |
| 1432 | `load_config()` | 경로 복사 → **삭제** |
| 1540, 1544 | `save_config()` | 위젯→`self.config` 대입 → **삭제** (이미 대입된 상태) |
| 1550 | `save_config()` | 경로 읽기 → `self.config` 참조 |
| 1723, 1725 | `capture_snapshot()` | URL 읽기 → `self.config` |
| 1788 | `enhance_prompt_only()` | URL 읽기 → `self.config` |
| 2029, 2034~2035 | `_set_ui_enabled()` | targets에서 3개 제거 |
| 2464, 2466 | `_show_settings_dialog()` | 숨겨진 위젯→다이얼로그 복사 → `self.config` 참조 |
| 2476, 2488 | `_show_settings_dialog()` | 다이얼로그→숨겨진 위젯 쓰기 → `self.config` 대입 |
| 2502 | `_show_settings_dialog()` | 폴더 선택 결과 → `self.config` 대입 |
| 2513, 2516, 2522 | `_show_settings_dialog()` | `on_load` 복사 → `self.config` 참조 |
| 2545~2556 | `_show_settings_dialog()` | `on_save_close` → `self.config` 대입 후 `save_config()` |

**`main.ui` / `main_ui.py`**: 컨테이너 블록 전체 삭제 (2567~2639 / 대응 블록)

> ⚠️ **동작 변화 없음 확인 필요**: 현재 위젯 참조도 "저장 전" 상태를 들고 있는 것은 동일하므로,
> **세션 내 미저장 값 유지라는 현재 동작이 그대로 보존**된다.
> 단, 2단계 완료 후 `self.config`가 곧 유일한 원본이 되므로 이후 변경 사항은 즉시 `save_config()` 대상이 된다.

**예상 작업량**: `main.py` 약 30곳 / `main.ui` 73줄 / `main_ui.py` 약 120줄

### 3단계 — 죽은 폴백 코드 제거 (위험도 🟢 낮음)

| 대상 | 라인 | 근거 |
|---|---|---|
| `_setup_theme_selector()` 폴백 분기 | 508~545 | `themeComboBox`가 `main.ui:115`에 존재해 506에서 `return` → **미실행 코드** |
| `_setup_help_tab()` 전체 | 1498~1535 | `helpBrowser` 표시 불가. `help_dialog_v2.ui`(2563 `_show_help_dialog`)가 담당 |
| `helpBrowser` 위젯 | `main.ui:2636`, `main_ui.py:113~116` | 그룹C |

**예상 작업량**: `main.py` 약 45줄 / `main.ui` 1줄 / `main_ui.py` 4줄

---

## 4. 단계별 검증

### 공통 검증 명령

```powershell
cd 'c:\Users\donian\Desktop\python\ComfyCraft_AI_Easy_Studio'
.\.venv\Scripts\python.exe -m py_compile main.py
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

**기준선: 20개 통과 (6.8s) → 단계마다 회귀 0건 유지**

### 단계별 추가 확인

| 단계 | 추가 검증 |
|---|---|
| 0 | `git status` 클린, `app_config.json.bak` 생성 확인 |
| 1 | ① 앱 실행 시 **크래시 없이 시작** (offscreen 프로브로 `MainController` 생성)<br>② 좌측 도움말 버튼 동작<br>③ 설정 버튼 → 설정창 열림 → 연결확인/폴더찾기/저장·닫기 모두 정상 |
| 2 | ① 설정창에서 URL 변경 → 저장 → **앱 재시작 후 값 유지** (`app_config.json` 확인)<br>② `app_config.json`에 불필요한 키가 생기지 않음<br>③ 모델 목록 자동 로드(시작 0.15초 후)가 새 URL로 동작 |
| 3 | ① 테마 드롭다운 정상 동작<br>② 좌측 도움말 버튼 정상 동작<br>③ 앱 시작 크래시 없음 |

### 신규 테스트 제안 (`tests/test_ui_cleanup.py`)

| # | 테스트 이름 | 검증 내용 |
|---|---|---|
| 1 | `test_hidden_widgets_removed_from_ui` | `main.ui`에 그룹B/C 위젯명이 남아있지 않음 |
| 2 | `test_controller_init_without_hidden_container` | offscreen `MainController` 생성 시 크래시 없음 |
| 3 | `test_config_url_is_source_of_truth` | 설정값이 `self.config`에서 직접 읽힘 (위젯 경유 없음) |

> `unittest` 기반 (pytest 미설치, 기존 관례 준수)

---

## 5. 위험 요소 및 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| `app_config.json` 손상 | 사용자 설정 소실 | 0단계에서 `.bak` 백업 + git 커밋, 단계 종료 후 `git diff`로 무결성 확인 |
| `is_lm_connected()` 판정 변화 | 프롬프트 향상 시 연결 재확인 트리거 | 아래 5.1 참조 — 1단계에선 **유지**, 2단계에서 `self.lm_connected` 단순화 |
| `update_model_path_status()` 표시 소실 | 경로 검증 메시지가 안 보임 | 이미 표시 불가 상태(부모 숨김). 설정창은 명시적 label 전달이라 영향 없음 |
| `settingsLayout` 폴백 제거 | 테마 드롭다운 소실 | `themeComboBox` 존재를 실행으로 확인(실측 True) 후에만 제거 |
| `main_ui.py` 손상 | 정적 참조 불일치 | `pyside6-uic`로 `main.ui`에서 재생성 가능, git으로 복원 가능 |
| XML 구조 오류로 UI 로드 실패 | 앱 시작 불가 | 단계마다 `py_compile` + `load_ui` 프로브 실행으로 즉시 검출 |

### 5.1 `is_lm_connected()` 세부 (⚠️ 2단계에서 반드시 처리)

**현재 코드 (1207~1211)**

```python
def is_lm_connected(self):
    if self.lm_connected:
        return True
    label = self.find(QLabel, "lmStatusLabel")      # ← 그룹C (표시 불가)
    return bool(label and "연결 성공" in label.text())
```

- `lmStatusLabel`은 **표시 불가**이므로 `label.text()`는 UI 갱신으로 바뀔 수 있고,
  실제 판정 근거는 사실상 `self.lm_connected`(1200~1201에서 설정)다.

**2단계 적용안**

```python
def is_lm_connected(self):
    return bool(self.lm_connected)
```

- 호출부: 1800 (`enhance_prompt_only`) — 생성 파이프라인 시작 전 연결 확인용
- `self.lm_connected`는 `apply_connection_result()`(1199~1201)에서만 변경되며,
  `check_connection()`(1169) 호출 시 `None`으로 초기화되어 재확인 흐름이 유지된다.
- **동등성 확보**: 1단계(라벨만 제거)에서는 라벨 분기를 **유지**하고,
  2단계에서 위와 같이 단순화한다. 두 단계로 나눈 이유가 이것이다.

---

## 6. 구현 순서 (체크리스트)

- [ ] **0단계** 작업 트리 커밋 + `app_config.json.bak` 생성
- [ ] **1-a** `main.ui` 그룹B/C 11개 위젯 제거
- [ ] **1-b** `main_ui.py` 동일 11개 제거
- [ ] **1-c** `main.py` 시그널 연결(340~359) · targets(2026~2056) 제거
- [ ] **1-d** `main.py` 상태 라벨 갱신(1041·1055) 제거
- [ ] **1-e** `is_lm_connected()` 라벨 분기 **유지** (2단계에서 처리)
- [ ] **1-f** `py_compile` + `unittest`(20) + `load_ui` 프로브
- [ ] **1-g** 1단계 기준선 커밋

- [ ] **2-a** `main.py` 읽기 지점 8곳 → `self.config` 참조
- [ ] **2-b** `main.py` 쓰기 지점 8곳 → `self.config` 대입
- [ ] **2-c** `save_config()` / `load_config()` 불필요 복사 제거
- [ ] **2-d** `is_lm_connected()` 단순화 + `update_model_path_status()` 기본 인자 정리
- [ ] **2-e** `main.ui` 컨테이너 블록 전체(73줄) 삭제
- [ ] **2-f** `main_ui.py` 컨테이너 생성 블록 전체 삭제
- [ ] **2-g** `py_compile` + `unittest` + `app_config.json` 무결성 확인
- [ ] **2-h** 2단계 기준선 커밋

- [ ] **3-a** `_setup_theme_selector()` 폴백 분기(508~545) 제거
- [ ] **3-b** `_setup_help_tab()`(1498~1535) 및 `helpBrowser` 제거
- [ ] **3-c** `py_compile` + `unittest` + 수동 3종(테마/도움말/시작)
- [ ] **3-d** `tests/test_ui_cleanup.py` 신규 작성
- [ ] **3-e** 최종 커밋

---

## 7. 이번 작업에서 변경하지 않는 것

- `assets/ui/settings_dialog.ui` / `settings_dialog_ui.py` (현행 설정창)
- `assets/ui/help_dialog_v2.ui` / `help_dialog_v2_ui.py` (현행 도움말)
- `themeComboBox` 및 `main.ui:115` 위치 (정상 노출 위젯)
- `lmStatusBtn` / `comfyStatusBtn` 배지 버튼 (실제 상태 표시 담당)
- `app/core/config_manager.py` — ConfigManager 싱글턴·저장 로직 유지
- `workflows/*.json` 템플릿 및 `workflows/app_config.json` (사용자 실설정)
- `app/sections/*.py`, `app/core/*.py` (이번 정리 대상 아님)
- `IMG2IMG_PLAN.md` (보류 상태 유지)
- 위젯 배치·레이아웃 구조 (죽은 UI 제거만, redesign 없음)
